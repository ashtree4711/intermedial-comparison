<nav class="navbar navbar-expand-md bg-light navbar-light shadow-sm fixed-top justify-content-between">
    <!-- Brand -->
    <a class="navbar-brand" href="/">Intermedialer Vergleich</a>
    <div class="w-100"></div>
    <!-- Toggler/collapsibe Button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar links -->
    <div class="collapse navbar-collapse d-flex justify-content-md-between" id="collapsibleNavbar">
        <ul class="nav navbar-nav">
            <li class="dropdown">
                {% if webInfo.comparison==true %}
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    {{ data.chunkInfo.work.name }} <small class="badge badge-primary">{{ data.chunkInfo.work.author }}</small>
                    <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    {% for chunk in data.chunks %}
                    <li><a href="{{ webInfo.baseurl }}chunks/{{ chunk.id }}"><small>Chunk: {{ chunk.chunk_no }}</small></a></li>

                    {% endfor %}
                </ul>
                {% endif %}
            </li>
        </ul>



    </div>
</nav>